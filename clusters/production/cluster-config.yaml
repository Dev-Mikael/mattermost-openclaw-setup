# AUTO-GENERATED by scripts/04-bootstrap-flux.sh — safe to commit.
# Real values are substituted at deploy time. This is a placeholder.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-values
  namespace: flux-system
data:
  DOMAIN: "modumichael.me"
  LETSENCRYPT_EMAIL: "admin@modumichael.me"
  CLUSTER_NAME: "production"
  S3_BUCKET_NAME: "REPLACE-AT-DEPLOY-TIME"
  AWS_REGION: "us-east-1"
  SECRET_PREFIX: "mattermost-openclaw-setup"
  MM_BOT_DM_POLICY: "pairing"
  MM_BOT_ALLOW_FROM: "[]"
